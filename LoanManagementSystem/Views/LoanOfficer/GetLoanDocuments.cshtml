@model IEnumerable<LoanManagementSystem.Models.RegistrationDocuments>

@{
    ViewBag.Title = "GetLoanDocuments";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>GetLoanDocuments</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.RegId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ImageUrl)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PublicId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.RegId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ImageUrl)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PublicId)
        </td>
        <td>
            @Html.ActionLink("Approve", "ApproveLoan", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Reject", "RejectLoan", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
