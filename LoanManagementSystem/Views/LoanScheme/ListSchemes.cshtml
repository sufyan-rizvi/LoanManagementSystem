
@{
    ViewBag.Title = "ListSchemes";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@model IEnumerable<LoanManagementSystem.Models.LoanScheme>

<h2>Available Loan Schemes</h2>

<table class="table">
    <thead>
        <tr>
            <th>Scheme Name</th>
            <th>Short Description</th>
            <th>Category</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var scheme in Model)
        {
            <tr>
                <td>@scheme.SchemeType</td>
                <td>@scheme.ShortDescription</td>
                <td>
                    @Html.ActionLink("Know More", "SchemeDetails", new { id = scheme.LoanSchemeId }, new { @class = "btn btn-primary" })
                </td>
            </tr>
        }
    </tbody>
</table>
